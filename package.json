{
  "name": "autometa",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "services/*"
  ],
  "devDependencies": {
    "typescript": "~6.0.2",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@testing-library/react": "^16.0.1",
    "jsdom": "^25.0.1",
    "vitest": "^2.1.3"
  },
  "scripts": {
    "test": "vitest run",
    "desktop": "bun --cwd apps/desktop dev",
    "desktop:build": "bun --cwd apps/desktop run build",
    "build:grading-template": "bun build packages/simulation-engine/src/index.ts --target=node --format=esm --banner=\"// AUTO-GENERATED from packages/simulation-engine/src/index.ts — do not edit; regenerate via 'bun run build:grading-template'.\" --outfile=docs/classroom-template/grading/simulation-engine.generated.mjs"
  }
}
