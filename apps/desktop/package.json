{
  "name": "desktop",
  "private": true,
  "version": "0.2.2",
  "type": "module",
  "scripts": {
    "dev": "node scripts/build-backend.cjs --if-missing && tauri dev",
    "build": "node scripts/build-backend.cjs && tauri build",
    "tauri": "tauri",
    "build:backend": "node scripts/build-backend.cjs"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.2.0",
    "@tauri-apps/plugin-updater": "^2.10.1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.2.0"
  }
}
